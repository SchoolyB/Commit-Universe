# When someone stars the repo, a cosmic event happens
name: ⭐ Star Triggers Cosmic Event

on:
  watch:
    types: [started]

jobs:
  cosmic-gift:
    name: 🌟 Starlight Event
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Universe
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 🔧 Configure Git
        run: |
          git config user.name "Commit Universe Engine"
          git config user.email "universe@commit-universe.dev"

      - name: 🌟 Generate Star Event
        run: |
          python -m engine.main \
            --universe . \
            --events 5

      - name: 📤 Push Changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git push
          fi
